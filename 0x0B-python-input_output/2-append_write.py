#!/usr/bin/python3

"""Defie rff drfeef ff fff refion."""


def append_write(filename="", text=""):
    """Apprfe edfcedf wefeef dfrf efref erferfer frefrefile.
    Args:
        filename (str): ddfcfc frefrf fref rfo.
        text (str): Thfv fvf fref referf frile.
    Returns:
        The numbffrfgr rrgrtg rgrtgtr rgtrnded.
    """
    with open(filename, "a", encoding="utf-8") as f:
        return f.write(text)
